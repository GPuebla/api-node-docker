<h1>Usuarios</h1>

<table border="1" cellpadding="8">
  <thead>
    <tr>
      <th>Email</th>
      <th>Rol</th>
      <th>Activo</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.role %></td>
        <td><%= user.isActive ? "Sí" : "No" %></td>
      </tr>
    <% }) %>
  </tbody>
</table>