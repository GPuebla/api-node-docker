# Usa una imagen oficial de Node
FROM node:18

# Crea y define el directorio de trabajo dentro del contenedor
WORKDIR /app

# Copia los archivos de dependencias
COPY package*.json ./

# Instala las dependencias (como en producción)
RUN npm install

# Instala nodemon globalmente (solo para desarrollo)
RUN npm install -g nodemon

# Copia el resto del código (aunque luego usaremos un volumen)
COPY . .

# Expone el puerto 3000 (para poder verlo en el navegador)
EXPOSE 3000

# Comando para iniciar el servidor en modo desarrollo
CMD ["nodemon", "index.js"]

